<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.CategoryMapper">

    <select id="selectEnabledList" resultType="com.blog.entity.Category">
        SELECT * FROM blog_category WHERE deleted = 0 ORDER BY sort ASC, id ASC
    </select>

    <select id="selectCategoryListWithCount" resultType="com.blog.entity.Category">
        SELECT c.*, COUNT(a.id) as article_count 
        FROM blog_category c 
        LEFT JOIN blog_article a ON c.id = a.category_id AND a.deleted = 0
        WHERE c.deleted = 0
        GROUP BY c.id
        ORDER BY c.sort ASC, c.id ASC
    </select>

</mapper>
